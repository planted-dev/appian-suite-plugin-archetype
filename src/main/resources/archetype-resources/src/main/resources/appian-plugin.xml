<appian-plugin name="${project.name}" key="${project.groupId}">
    <plugin-info>
        <description>${project.description}</description>
        <vendor url="${project.url}" />
        <version>${project.version}</version>
    </plugin-info>
    <!-- Functions -->
    <function key="appianFunction" class="${project.groupId}.Function" />
    <!-- Smart Services -->
	<smart-service name="Appian Smart Service" key="appianSmartService" class="${project.groupId}.SmartService" /> 
</appian-plugin>